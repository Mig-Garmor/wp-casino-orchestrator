# WP Casino

Local WordPress development project using Docker, Nginx, PHP-FPM, MySQL, WP-CLI, and VS Code Dev Containers.

## Architecture

Browser -> Nginx -> PHP-FPM WordPress -> MySQL

## Setup

Copy environment file:

```bash
cp .env.example .env
```
